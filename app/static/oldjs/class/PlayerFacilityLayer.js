define([
  // Libs
  "jquery",
  "TileMapLayer"
], function ($, TileMapLayer) {
  return (
    class PlayerFacilityLayer extends TileMapLayer {
      constructor(parms = {}) {
        super(parms);
      }

    });
});
