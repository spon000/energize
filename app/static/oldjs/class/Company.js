define([
  // Libs
  "jquery",
  "Vector2",
  "DialogBoxInfo",
  "easeljs",

], function ($, Vector2, DialogInfoBox, easeljs) {

  return (

    // Class that represents a city.
    class Company {

      constructor(id) {
        this._id = companyParms.id;
      }

      get company() {
        return new Promise(resolve)
      }
    });
});

